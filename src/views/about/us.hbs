<div class="container p-4">
    <div class="row">
        {{#each information}}
        <div class="col-md-4">
            <div class="card text-center">
                <div class="card-body">
                    <a href="{{INFORMACION_URL}}" target="_blank">
                        <img src="{{INFORMACION_URL}}" class="card-img-top text-center" alt="Card image cap">
                    </a>
                    <p class="m-2">{{INFORMACION_DESCRIPCION}}</p>
                </div>
            </div>
        </div>
        {{else}}
        <div class="col-md-4 mx-auto">
            <div class="card card-body text-center">
                <p>Disculpe las molestias, por el momento no existe información relacionada con la pagina</p>
            </div>
        </div>
        {{/each}}
    </div>
</div>